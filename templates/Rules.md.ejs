# 翻译规则列表

共有两种规则：

1. 术语表规则：检查翻译内容是否符合术语表；
2. 正则表达式规则：用正则表达式检查翻译内容是否符合规则。

**正则表达式规则列表**

词 | 翻译
-- | -----------
<% for (let key in rules.items) { %><%- this.escapeRule(key) %> | <%- this.escapeRule(rules.items[key]) %>
<% } %>

**正则表达式规则例外列表**

词 | 翻译
-- | -----------
<% for (let key in rules.exceptions) { %><% for (let item of rules.exceptions[key]) { %><%- this.escapeRule(key) %> | <%= item %>
<% } %><% } %>
